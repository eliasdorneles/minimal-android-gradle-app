Minimal Gradle Android app
==========================

This is a minimal Android app using Gradle.


**Requirements**

* JDK
* Android SDK


Build the app with:

    ./gradlew build


Install the app in an emulator (has to be already running) or device:

    ./gradlew installDebug

